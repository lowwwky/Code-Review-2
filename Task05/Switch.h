#pragma once

#include <list>

/**
 * Класс для управления операциями со списками целых чисел.
 *
 * Предоставляет набор методов для выполнения основных операций над списками:
 * - Заполнение списков различными способами (ручной, случайный, из файла)
 * - Вывод содержимого списков на экран
 * - Специализированные операции согласно заданию (перенос среднего элемента)
 *
 * Класс не хранит состояние, все методы являются статическими по своей сути
 * и работают с переданными списками через параметры. Это позволяет использовать
 * его как утилитарный класс для работы с контейнерами std::list.
 *
 * Все методы включают проверку корректности входных данных и обрабатывают
 * возможные ошибки (некорректный ввод, проблемы с открытием файлов).
 */
class SwitchDigits {
public:
	// FIX_ME: в Google Codestyle Guide названия переменных пишутся как lower_case_with_underscores.
	/*void input_data(std::list<int>& L, int n);
	void print_data(const std::list<int> L);
	void splice_data(std::list <int>& L1, std::list <int>& L2);*/

	void input_data(std::list<int>& L, int n);
	void print_data(const std::list<int> L);
	void splice_data(std::list <int>& L1, std::list <int>& L2);
};
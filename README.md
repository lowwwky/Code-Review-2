# Code-Review-2
# Выполняла: Кужлева Любовь ИТ-17,18
# Вариант 2.

В ходе ревью исходного кода были выявлены две основные проблемы, характерные для большинства заданий:

Глобальное использование пространства имен (using namespace std;). Это считается плохой практикой,
так как переносит все имена из стандартной библиотеки в глобальную область видимости.
Это может привести к конфликтам имен, снижению читаемости кода и трудноуловимым ошибкам, 
особенно в больших проектах или при обновлении стандартов языка.

Отсутствие декомпозиции. Большая часть кода выполнялась в функции main(), что затрудняет чтение, 
тестирование, отладку и дальнейшее использование отдельных алгоритмов в других проектах.

# Задание 1: Работа со стеком (добавление элемента)

## Описание задачи
Имеется указатель P1 на вершину непустого стека. Элементы стека представлены в виде 
связанных узлов-записей типа TNode, где каждый узел содержит целочисленное значение Data 
и указатель Next на следующий элемент. Вершиной стека считается первый элемент цепочки, 
а поле Next последнего элемента равно nullptr. Требуется добавить новый элемент со 
значением D в стек и вывести адрес P2 новой вершины стека. Известно, что стек непустой.

**Входные данные** (ввод с клавиатуры):
- Целое число D — значение для нового элемента
- Последовательность целых чисел для формирования начального стека (признак окончания 
  ввода — ввод символа 'q')

**Выходные данные:**
- Адрес P2 новой вершины стека (после добавления элемента)
- Содержимое стека после добавления элемента

## Что было улучшено

### ✅ Декомпозиция
- Весь код был вынесен из `main()` в отдельные методы
- Каждый метод решает только одну задачу
- Методы перенесены в различные классы

### ✅ Стиль кодирования (Google C++ Style)
- Исправлены неинформативные названия переменных:
  - `N` → `amount_of_elements`
  - `showAddress` → `show_address` 
- Убрано использование `using namespace std;`
- Код приведен к единому стилю форматирования

### ✅ Безопасность и читаемость
- Добавлена проверка входных данных
- Добавлены комментарии `FIX_ME`, указывающие на потенциальные проблемные места

### ✅ Платформонезависимость
- Убраны Windows-специфичные функции (`SetConsoleCP`, `SetConsoleOutputCP`)
- Оставлен кроссплатформенный `setlocale(LC_ALL, "Russian.utf8")`

# Задание 2: Работа с очередью (извлечение элементов)

## Описание задачи
Дано число N (> 0) и указатели P1 и P2 на начало и конец непустой очереди. 
Очередь моделируется цепочкой связанных узлов-записей типа TNode. Требуется 
извлечь из очереди N начальных элементов и вывести их значения. Если очередь 
содержит менее N элементов, то извлечь все ее элементы. После извлечения 
элементов необходимо освобождать память, которую они занимали.

**Входные данные** (ввод с клавиатуры):
- Целое число N — количество элементов для извлечения (N > 0)
- Последовательность целых чисел для формирования начальной очереди (признак окончания 
  ввода — ввод символа 'q')

**Выходные данные:**
- Значения извлеченных элементов (в порядке их извлечения из очереди)
- Новые адреса начала и конца очереди (для пустой очереди дважды вывести nullptr)

## Что было улучшено

### ✅ Декомпозиция
- Весь код был вынесен из `main()` в отдельные методы
- Каждый метод решает только одну задачу
- Методы перенесены в различные классы

### ✅ Стиль кодирования (Google C++ Style)
- Исправлены неинформативные названия переменных:
  - `m,n` → `amount_of_digits` / `amount_to_delete` 
  - `show1` → `show_source` 
- Убрано использование `using namespace std;`
- Код приведен к единому стилю форматирования

### ✅ Безопасность и читаемость
- Добавлена проверка входных данных
- Добавлены комментарии `FIX_ME`, указывающие на потенциальные проблемные места

### ✅ Платформонезависимость
- Убраны Windows-специфичные функции (`SetConsoleCP`, `SetConsoleOutputCP`)
- Оставлен кроссплатформенный `setlocale(LC_ALL, "Russian.utf8")`


# Задание 3: Работа с односвязным списком (копирование и вставка)

## Описание задачи
Дан односвязный линейный список и указатель на голову списка P1. Значения элементов списка 
упорядочены по возрастанию. Необходимо создать копию исходного списка, после чего во вновь 
созданном списке вставить значение M так, чтобы он остался упорядоченным. Требуется вывести 
ссылку на первый элемент полученного списка P2.

**Входные данные** (ввод с клавиатуры):
- Целое число M — значение для вставки в новый список
- Последовательность целых чисел для формирования исходного упорядоченного списка 
  (признак окончания ввода — ввод символа 'q')

**Выходные данные:**
- Исходный список (значения и адреса элементов)
- Адрес P1 головы исходного списка
- Новый список-копия после вставки элемента M
- Адрес P2 головы нового списка

## Что было улучшено

### ✅ Декомпозиция
- Весь код был вынесен из `main()` в отдельные методы
- Каждый метод решает только одну задачу
- Методы перенесены в различные классы

### ✅ Стиль кодирования (Google C++ Style)
- Исправлены неинформативные названия переменных:
  - `m,n` → `digits_to_add` / `amount_of_elements` 
  - `DeletNode` → `delete_node` 
- Убрано использование `using namespace std;`
- Код приведен к единому стилю форматирования

### ✅ Безопасность и читаемость
- Добавлена проверка входных данных
- Добавлены комментарии `FIX_ME`, указывающие на потенциальные проблемные места

# Задание 4: Двусвязный список (вывод в файл в обратном порядке с удалением)

## Описание задачи
Дан двусвязный линейный список и указатель на первый элемент этого списка. 
Необходимо создать текстовый файл, в который вывести содержимое списка в обратном порядке. 
В процессе вывода элементов они должны удаляться из списка. Числа выводятся в одну строку, 
разделяя ровно одним пробелом. В конце строки пробел не ставится.

**Входные данные** (ввод с клавиатуры):
- Имя выходного файла (например, "output.txt" или "result.txt")
- Последовательность целых чисел для формирования исходного двусвязного списка 
  (признак окончания ввода — ввод символа 'q')

**Выходные данные:**
- Содержимое списка (выводится на экран для контроля)
- Текстовый файл с элементами списка в обратном порядке
- Подтверждение успешной записи в файл

## Что было улучшено

### ✅ Декомпозиция
- Весь код был вынесен из `main()` в отдельные методы
- Каждый метод решает только одну задачу
- Методы перенесены в различные классы

### ✅ Стиль кодирования (Google C++ Style)
- Исправлены неинформативные названия переменных:
  - `ImyaFaila` → `file_name`
  - `VvodChisla` → `number_input` 
- Убрано использование `using namespace std;`
- Код приведен к единому стилю форматирования

### ✅ Безопасность и читаемость
- Добавлена проверка входных данных
- Добавлены комментарии `FIX_ME`, указывающие на потенциальные проблемные места

# Задание 5: Работа с контейнером list (splice)

## Описание задачи
Даны списки list1 и list2 (контейнеры list из STL). Список list1 имеет нечетное количество элементов. 
Необходимо переместить средний элемент списка list1 в конец списка list2, используя один вызов 
функции-члена splice().

**Входные данные** (различные способы ввода):
- Выбор способа заполнения контейнеров:
  - С клавиатуры (ручной ввод)
  - Рандом (генерация случайных чисел)
  - Из файла (чтение из текстового файла)
- Для каждого способа ввода:
  - Элементы для списка list1 (нечетное количество)
  - Элементы для списка list2 (любое количество)

**Выходные данные:**
- Исходное состояние обоих списков
- Информация о том, какой элемент является средним в list1
- Состояние обоих списков после перемещения среднего элемента

## Что было улучшено

### ✅ Декомпозиция
- Весь код был вынесен из `main()` в отдельные методы
- Каждый метод решает только одну задачу
- Методы перенесены в различные классы

### ✅ Стиль кодирования (Google C++ Style)
- Исправлены неинформативные названия переменных:
  - `m` → `filling_type`
- Убрано использование `using namespace std;`
- Код приведен к единому стилю форматирования

### ✅ Безопасность и читаемость
- Добавлена проверка входных данных
- Добавлены комментарии `FIX_ME`, указывающие на потенциальные проблемные места
